"""Independently owned story-script modes.

Add a new mode by creating a module here that exports MODE. The registry
discovers modules automatically, so contributors should not edit a shared
mode list or the shared system prompt for mode-specific work.
"""
